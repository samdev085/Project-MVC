@model Notice
@{
}

<div class="text-center">
    <H1>Delete Tag</H1>

    <H3>You realy delete a Notice (@Model.Id)</H3>

    <hr>

    <label>Notice Title</label>
    <H5>@Model.Title</H5>

    
    <label>Notice Text</label>
    <H5>@Model.Text</H5>
</div>

<a asp-route-id="@Model.Id" asp-controller="Home" asp-action="DeleteNoticeConfirm" class="btn btn-primary" type="submit" role="button">Confirm Delete</a>
